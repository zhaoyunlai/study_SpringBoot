spring:
#  mvc:
#    static-path-pattern: /res/**

  resources:
    static-locations: [classpath:/haha]
    add-mappings: true
    cache:
      period: 11000
  mvc:
    hiddenmethod:
      filter:
        enabled: true
